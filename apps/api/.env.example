# Only needed when running the API outside Docker Compose (npm run dev).
NODE_ENV=development
PORT=4000
MONGODB_URI=mongodb://isp888:devpassword@localhost:27017/isp888?authSource=admin
CORS_ORIGIN=http://localhost:3000
